extern crate semver;
use self::semver::Version;

/** 
 * A documentation comment 
 */
#[cfg(target_os="windows")]
fn my_func() {
    const MESSAGE: & 'static str = "Hello World!";
    println!("Message is: {}", MESSAGE); // A simple line comment
    
    let current = Version::parse("1.2.3");
    let x : u32 = 123;
    let mut x = true;
}